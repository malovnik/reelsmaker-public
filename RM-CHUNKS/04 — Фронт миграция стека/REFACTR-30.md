# REFACTR-30 — Миграция shell-компонентов (AppShell, NavRail, TopBar)

> **Этап:** 04
> **Шаг:** 31 из 67
> **Зависимости:** REFACTR-28 (роутинг), REFACTR-29 (Query).
> **Следующий шаг:** REFACTR-31 (Удаление Next.js)

---

## Роли

### R-FRONTEND-ARCHITECT
**Soul:** Shell — то, что видит пользователь всегда. Перенос должен сохранить UX старой версии, но через новые примитивы.

### R-UX-WRITER
**Soul:** Каждая надпись в Shell — на русском, проверена на клише.

---

## ТРИЗ-принцип

*Принцип копирования.* Компоненты `apps/frontend/src/components/shell/*` переносим 1:1 (код), меняем только импорты (Next.js → TanStack).

---

## Оркестрация

**Режим:** Sequential.

---

## Микрозадачи

### 30.1 AppShell.tsx

- [ ] Скопировать из `apps/frontend/src/components/shell/AppShell.tsx`.
- [ ] Заменить `next/link` на `@tanstack/react-router Link`.
- [ ] Заменить `usePathname` на `useRouterState({ select: s => s.location.pathname })`.
- [ ] Подключить в `__root.tsx`.

### 30.2 NavRail.tsx

- [ ] Скопировать.
- [ ] Обновить импорты.
- [ ] Адаптивность — проверить breakpoints (Tailwind 4 синтаксис).

### 30.3 TopBar.tsx

- [ ] Скопировать.
- [ ] Обновить импорты.
- [ ] Поиск Cmd+K — **НЕ** чиним в этом чанке (это REFACTR-57). Здесь — placeholder.

### 30.4 Подключить к `__root.tsx`

```tsx
export const Route = createRootRoute({
  component: () => (
    <AppShell>
      <Outlet />
    </AppShell>
  ),
});
```

### 30.5 Прогнать все 19 маршрутов

- [ ] Открыть каждый, проверить что shell отрисовывается.
- [ ] Нет console.error.
- [ ] Навигация в NavRail работает.

### 30.6 Миграция минимального набора страниц

Перенести 2 ключевых роута полностью, чтобы показать паттерн (остальные будут переноситься в этапах 06-08 с новым дизайном):

- [ ] `/` (главная) — перенос `HomeClient.tsx` как промежуточный (будет переделан в Студию в REFACTR-39+).
- [ ] `/jobs/:id` — перенос `JobDetailClient.tsx` (будет переделан в Workbench в REFACTR-45+).

### 30.7 Smoke

- [ ] Приложение целиком работает: открыть главную → увидеть проекты → кликнуть → открыть детали job → вернуться.
- [ ] TanStack Query devtools показывает кеш.

### 30.8 Commit + Serena

---

## GATE-чекпоинт

- [ ] AppShell, NavRail, TopBar работают в Vite-версии.
- [ ] Все 19 маршрутов открываются с shell-обёрткой.
- [ ] 2 ключевых роута (/, /jobs/:id) имеют реальный контент (промежуточный).
- [ ] Console чист.

---

## Артефакт на выходе

Shell-компоненты в Vite-проекте + 2 мигрированные страницы.
