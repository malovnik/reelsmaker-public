# REFACTR-31 — Удаление Next.js, верификация памяти

> **Этап:** 04
> **Шаг:** 32 из 67
> **Зависимости:** REFACTR-27..30.
> **Следующий шаг:** REFACTR-32 (Дизайн-система: принципы)

---

## Роли

### R-FRONTEND-ARCHITECT
**Soul:** Миграция завершена только когда старое удалено. Пока оба стека параллельно — технический долг.

### R-DEVOPS
**Soul:** Обновить `run.sh` — синхронизация запуска.

### R-DEVIL
**Soul:** До удаления — бэкап. После удаления — тесты.

---

## ТРИЗ-принцип

*Принцип отбрасывания.* Всё что не Vite — прочь. Никаких .next/ артефактов, никаких next-dev зависимостей. Чистая миграция.

---

## Оркестрация

**Режим:** Sequential.

---

## Микрозадачи

### 31.1 Бэкап старого фронта

- [ ] `git commit` текущего состояния с тегом `pre-nextjs-removal`.

### 31.2 Переименование директорий

- [ ] `apps/frontend/` → `apps/frontend-legacy/` (временно).
- [ ] `apps/frontend-vite/` → `apps/frontend/`.

### 31.3 Обновить корневые конфиги

- [ ] `pnpm-workspace.yaml` — обновить пути.
- [ ] `run.sh` — сменить `cd apps/frontend && pnpm dev` на работу с Vite (в целом путь тот же).
- [ ] `package.json` корневой — если есть ссылки на `apps/frontend-legacy`.

### 31.4 Удалить Next.js-специфику из package.json

- [ ] `next`, `eslint-config-next` — drop.
- [ ] `scripts.dev`: убрать `NODE_OPTIONS='--max-old-space-size=12288'`, оставить `vite`.
- [ ] `scripts.build`: `vite build`.

### 31.5 Smoke после удаления

- [ ] `./run.sh` стартует.
- [ ] Backend доступен на :8000, frontend на :3000.
- [ ] Все 19 маршрутов работают.
- [ ] SSE прогресс работает.

### 31.6 Замер RAM

- [ ] `ps -o rss -p $(pgrep -f "vite dev")` — цифра в memory.
- [ ] **Цель: idle <500 МБ, peak под нагрузкой <1 ГБ**.
- [ ] Сравнить со старой цифрой (Next.js ест 6-12 ГБ из-за 12288 MB лимита).

### 31.7 Удаление legacy

- [ ] Если все тесты зелёные — `rm -rf apps/frontend-legacy`.
- [ ] Если нет — **СТОП**, фикс, retry. Не удалять пока не работает.

### 31.8 Обновить `.gitignore`

- [ ] Убрать `.next/`.
- [ ] Добавить `apps/frontend/dist/`, `apps/frontend/node_modules/`.

### 31.9 Документация

- [ ] Обновить `CLAUDE.md` (frontend раздел).
- [ ] Обновить `README.md` (команды запуска).

### 31.10 Commit + лог

### 31.11 Итог Этапа 04

- [ ] В `PIPELINE-НАВИГАТОР.md` Лог: «Этап 04 ЗАВЕРШЁН. Миграция на Vite завершена. RAM снижен с 12 ГБ до XXX МБ.»

---

## GATE-чекпоинт

- [ ] Next.js полностью удалён.
- [ ] Vite-проект в `apps/frontend/`.
- [ ] `./run.sh` работает.
- [ ] RAM <500 МБ idle.
- [ ] Все 19 маршрутов открываются.
- [ ] Git тег + changelog.
- [ ] **Этап 04 ЗАВЕРШЁН.**

**СТОП если:** после удаления legacy какой-то сценарий сломался — откат через git (есть тег `pre-nextjs-removal`), фикс, retry.

---

## Артефакт на выходе

Чистый Vite-проект, Next.js удалён, RAM в норме, все маршруты работают.
