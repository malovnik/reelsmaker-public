# Scan 1 — Secret Leak Audit (private → public)

**Repo:** `/Users/malovnik/Documents/Dev/reelsmaker-public`
**Date:** 2026-05-27
**Scope:** 676 tracked files, 23 commits, working tree clean.
**Auditor role:** Secret Leak Auditor (паранойя к утечкам, исчерпывающность).

---

## ВЕРДИКТ: ⛔ ОПАСНО для публикации

**Найден 1 РЕАЛЬНЫЙ секрет в трекаемом файле. Публиковать НЕЛЬЗЯ до ремедиации.**

---

## НАЙДЕНЫ ЛИ РЕАЛЬНЫЕ СЕКРЕТЫ: ДА (1)

### 🔴 CRITICAL — Zhipu (Z.AI / GLM) API key

| Поле | Значение |
|------|----------|
| Файл | `docs/superpowers/plans/2026-04-19-glm-integration.md` |
| Строка | **844** |
| Значение | `ZHIPU_API_KEY=<REDACTED-ZHIPU-KEY>` |
| Формат | Каноничный Zhipu: `<32-hex-id>.<16-char-secret>` — совпадает с описанием в `.env.example` (`# Ключ формата <id>.<secret>`) |
| Вердикт | **РЕАЛЬНЫЙ** (не плейсхолдер) |

**Почему реальный, а не плейсхолдер:**
- Полный валидный формат ключа Zhipu (id.secret), не обрезан и не маскирован.
- В ТОМ ЖЕ документе на строках 798 и 911 ключ намеренно сокращён до плейсхолдера `f639...` — значит автор знал про секретность, но строку 844 (пример команды smoke-теста) оставил с полным ключом.
- Значение уникально (не встречается в `.env.example`, где `ZHIPU_API_KEY=` пустой).

**Git-история:** ключ присутствует только в одном коммите — `2880063` ("Initial import: clean snapshot ... no secrets"). То есть история НЕ грязная сверх этого файла, но ремедиация через простое удаление строки оставит ключ в истории коммита `2880063`.

### Требуемая ремедиация (до публикации):
1. **ОТОЗВАТЬ/РОТИРОВАТЬ ключ** на https://open.bigmodel.cn/usercenter/apikeys — приоритет №1, ключ нужно считать скомпрометированным.
2. Заменить значение в строке 844 на плейсхолдер (`f639...` как в остальных местах файла).
3. Так как ключ в `2880063` (initial commit, 23 коммита всего): либо переписать историю (`git filter-repo`/squash), либо — раз это initial import — пересоздать чистый initial commit. После ротации (п.1) риск нейтрализован даже если строка останется в истории, но историю всё равно желательно вычистить.

---

## ПОДОЗРИТЕЛЬНЫЕ (проверены) → все ПЛЕЙСХОЛДЕРЫ/ЛОЖНЫЕ

| Где | Что | Вердикт |
|-----|-----|---------|
| `RM-CHUNKS/03 .../REFACTR-24.md:63` | `key='sk-abcdef123456789012345'` | ПЛЕЙСХОЛДЕР — пример лог-редактирования, явно фейковый |
| `.env.example` (весь файл) | `GEMINI_API_KEY=`, `ANTHROPIC_API_KEY=`, `OPENAI_API_KEY=`, `ZHIPU_API_KEY=`, `DEEPGRAM_API_KEY=` — все пустые | ПЛЕЙСХОЛДЕР — корректный шаблон, реальных значений нет |
| `docs/superpowers/plans/...glm-integration.md` строки 185, 798, 911 | `ZHIPU_API_KEY=` (пусто) и `f639...` (обрезано) | ПЛЕЙСХОЛДЕР |
| `apps/backend/.../factory.py`, `docs/.../glm-integration.md` (`api_key=cfg.xxx_api_key`) | ссылки на конфиг-объекты | НЕ СЕКРЕТ — читают из env/config |
| `run.sh:22` | строка-подсказка «добавь API-ключи» | НЕ СЕКРЕТ |

---

## ЧТО ПРОВЕРЕНО (исчерпывающий чеклист)

1. ✅ Трекаемые файлы с опасными именами (`.env`, `*secret*`, `*credential*`, `.pem`, `.key`, `.p12`, `.pfx`, `id_rsa`): найден только `.env.example` (легитимный шаблон). Реального `.env` НЕТ.
2. ✅ Паттерны ключей по всем 676 трекаемым файлам:
   - Gemini/Google `AIza...` — **0**
   - OpenAI `sk-...` — 0 реальных (1 плейсхолдер)
   - Anthropic `sk-ant-...` — **0**
   - AWS `AKIA...` — **0**
   - GitHub `ghp_/gho_/...` — **0**
   - Приватные ключи `BEGIN ... PRIVATE KEY` — **0**
   - JWT `eyJ....eyJ....sig` — **0**
   - Bearer-токены — **0**
   - Deepgram — **0**
   - Zhipu `<32hex>.<16char>` — **1 РЕАЛЬНЫЙ** (см. выше)
   - Пароли в URL `://user:pass@` — **0**
   - Publer / refresh_token / access_token / client_secret — **0**
3. ✅ Конфиги/код (`.py/.ts/.tsx/.js/.json/.yaml/.yml/.sh/.env*`): хардкод-присваивания секретов с длинными значениями — **0**. Все `api_key=` читаются из config/env. `run.sh` чист. `vite.config.ts` чист. RM-CHUNKS/ и docs/ (кроме строки 844) — чисты.
4. ✅ High-entropy base64-строки в коде — **0**.
5. ✅ Git-история (`git log -p --all`): единственное совпадение реального ключа — та же строка 844 в initial commit `2880063`. Прочих секретов в истории нет.
6. ✅ Доп. credential-файлы (`.npmrc`, `.netrc`, `.pgpass`, `.htpasswd`, service-account JSON, firebase) — **не трекаются**.

---

## ИТОГ

- **Реальные секреты:** 1 (Zhipu API key, `docs/superpowers/plans/2026-04-19-glm-integration.md:844`).
- **Вердикт:** ⛔ **ОПАСНО** — публикация запрещена до: (1) ротации ключа, (2) удаления значения из файла, (3) чистки git-истории.
- После выполнения ремедиации повторить этот скан для подтверждения **БЕЗОПАСНО**.
