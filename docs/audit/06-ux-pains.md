# 06 — UX-боли: карта с техническим доказательством и планом лечения

> **Этап:** 00 — Исследование и аудит (чанк 7/67, REFACTR-06)
> **Дата аудита:** 2026-04-24
> **Роли:** R-AUDITOR (ведущая), R-UX-WRITER (консультативно)
> **Входы:** `RM-CHUNKS/README.md`, `RM-CHUNKS/task.md §1`, `docs/audit/01-frontend-map.md`, `docs/audit/02-settings-inventory.md`, `docs/audit/05-pipeline-stages.md`.

---

## 0. Резюме

Собрано **11 болей** (6 от владельца + 5 выявленных в ходе аудита). Для каждой зафиксирована цитата/сценарий, техническое доказательство (`file:line`), способ воспроизведения, метрика и этап лечения. Все 11 сопоставлены с конкретными REFACTR-чанками плана.

**Топ-3 по остроте** (для очереди после аудита):

1. **OOM 12 ГБ в dev-фронте** — блокирует нормальную работу на 24 GB MacBook; лечится в первую очередь после архитектурных ADR (этап 04).
2. **Нет автосохранения + нет рестарта pipeline с шага X** — данные теряются, 60-минутное видео перекатывается с нуля при любом сбое. Лечится в этапе 02 (REFACTR-14 + REFACTR-16).
3. **Страница `/settings/subtitles` с horizontal scroll и смешанной палитрой** — дословно «омерзительно»; ломает единственный экспертный экран настройки стилей. Лечится в этапе 08 (REFACTR-52).

---

## 1. Боли владельца (прямые цитаты)

### Боль 1 — «Жрёт 12 ГБ RAM в dev»

| Параметр | Значение |
|---|---|
| **Тип** | Технический (производительность) |
| **Цитата** | «Фронт жрёт 12 ГБ RAM в dev ... OOM при забитой памяти» (`RM-CHUNKS/README.md:16`) |
| **Доказательство** | `apps/frontend/package.json:6` — `"dev": "NODE_OPTIONS='--max-old-space-size=12288' next dev"` |
| **Воспроизведение** | `cd apps/frontend && pnpm dev` → открыть Activity Monitor → node-процесс расходует ≥4 ГБ идля, ≥8–12 ГБ при навигации по страницам. |
| **Метрика** | `max-old-space-size=12288` МБ = 12 ГБ heap ceiling. Без флага Node.js падает с OOM до того, как dev-сервер запустится. |
| **Корень** | Next.js 16 + App Router + Turbopack + 8 больших client-компонентов (`SubtitleStyleEditor.tsx` 653 строки, `SubtitleSettingsClient.tsx` 458 строк, `PostProductionSettingsClient.tsx` 429 строк) + React 19. Single-user локальный SPA не нуждается в SSR/ISR/streaming — оверкилл по мотивации (task.md §1.1). |
| **Этап лечения** | **04 — Фронт: миграция стека** |
| **Целевой чанк** | `REFACTR-27` (ADR Vite 6), `REFACTR-28..30` (bootstrap Vite + TanStack Router/Query), `REFACTR-31` (цель ≤500 МБ idle, удалить `NODE_OPTIONS`). |

---

### Боль 2 — «Где-то светлые, где-то тёмные шрифты говно»

| Параметр | Значение |
|---|---|
| **Тип** | Визуальный (дизайн-система) |
| **Цитата** | «смешанные тёмные/светлые шрифты, где-то читаемо, где-то нет, нет цельной темы, нет persist» (`RM-CHUNKS/README.md:17`) |
| **Доказательство (light-mode протечки на тёмном фоне)** | — `SubtitleSettingsClient.tsx:343,357` — `border-red-200 bg-red-50 text-red-900` (Tailwind light scale на тёмном `--ink` фоне). <br/>— `PostProductionSettingsClient.tsx:368` — тот же error-bar. <br/>— `MoondreamSettings.tsx:147`, `UploadWizard.tsx:135,788`, `JobHero.tsx:118,142`, `JobCard.tsx:40,42`, `TinderClient.tsx:384,415` — ещё 9 точек `red-50/red-200/red-900`. |
| **Доказательство (hardcoded)** | — `SplitScreenPreviewEditor.tsx:103,297` — `color:"#fff"`, `background:"#000"`. <br/>— `SubtitlePreview.tsx:367,371,379,383,522,570,623` — `bg-black`, `bg-sky-500/85`, `bg-red-600/80`. <br/>— 27 использований tailwind-скал `stone-/zinc-/slate-/neutral-/gray-` против OKLCH токенов в `globals.css`. |
| **Воспроизведение** | Перейти на `/settings/subtitles` → спровоцировать ошибку (ввести невалидный JSON) → баннер ошибки появляется светло-розовым на тёмном фоне — нарушает иерархию и режет глаз. Выбрать пресет с невалидным hex — aналогично. |
| **Метрика** | **13+ компонентов** с hardcoded light-колорами на dark-теме. **27 mismatched** tailwind-скал против OKLCH design-system токенов. |
| **Корень** | `globals.css:10` декларирует единственную тёмную тему (`color-scheme: dark`), но компоненты времён переходных фаз 1–8 («stone-era», см. комментарий в `globals.css:45`) не были перемаплены на семантические токены `--danger`, `--success`, `--warning`. Нет persist темы на бэке — task.md §2.3 требует light + dark с CSS variables и localStorage. |
| **Этап лечения** | **05 — Фронт: дизайн-система и темы** |
| **Целевой чанк** | `REFACTR-33` (tokens + дуо-тема dark+light + persist), `REFACTR-34` (атомы/молекулы на токенах), `REFACTR-37` (рефакторинг всех компонентов под токены, удалить red-50/bg-black/stone-*). |

---

### Боль 3 — «Поиск по Cmd+K вверху не работает»

| Параметр | Значение |
|---|---|
| **Тип** | Функциональный |
| **Цитата** | «Cmd+K не работает» (`RM-CHUNKS/README.md:22`, `task.md §1.4`) |
| **Доказательство** | `apps/frontend/src/components/shell/TopBar.tsx:90–111` — JSX с иконкой поиска, placeholder «Найти проект, клип, момент…» и `<span className="kbd">⌘K</span>`. **Ни одного `onClick`, ни одного `onKeyDown`, ни одного `useHotkey`, ни одного Command Palette компонента во всём репозитории**: `grep -rn "Cmd+K\|cmdk\|Command.Palette\|CommandPalette" apps/frontend/src/` = 0 совпадений. Только визуальный placeholder без handler-ов. |
| **Воспроизведение** | Открыть любую страницу → кликнуть по полю поиска в TopBar → ничего не происходит. Нажать Cmd+K → ничего не происходит. |
| **Метрика** | **0** обработчиков на поле поиска. **0** палитр команд. **0** fuzzy-search индексов на фронте. |
| **Корень** | Декларативный TopBar-placeholder из фазы 9 redesign, но фактическая реализация команды откладывалась. |
| **Этап лечения** | **08 — Фронт: настройки + Cmd+K** |
| **Целевой чанк** | `REFACTR-57` (command-palette на cmdk/kbar + fuzzy + context-aware scopes: projects/clips/settings/commands). |

---

### Боль 4 — «Страница субтитров ужасно выглядит, горизонтальная прокрутка прям омерзительно»

| Параметр | Значение |
|---|---|
| **Тип** | Визуальный + адаптивность (IA) |
| **Цитата** | «Страница `/settings/subtitles` с horizontal scroll — "омерзительно-омерзительно-омерзительно"» (`task.md §1.3`) |
| **Доказательство** | — `SubtitleStyleEditor.tsx` — 653 строки одного компонента с 10+ секциями (цвет/обводка/шрифт/позиция/анимация/wordsPerLine…) подряд. <br/>— `SubtitleSettingsClient.tsx` — 458 строк: 4 больших колонки пресетов, preview, список, JSON-редактор + error-баннер (строка 343) light-mode в тёмной теме. <br/>— Нет `max-w-*` / `flex-wrap` / `grid-template-columns: repeat(auto-fit, minmax(...))` — на ширинах < 1100 px содержимое не помещается и переносится в `overflow-x`-прокрутку родительского wrapper-а. <br/>— `overflow-x-auto` напрямую зафиксирован в соседних разделах: `post-production/SplitScreenSection.tsx:181`, `upload/UploadWizard.tsx:496`, `scheduler/CampaignDetailClient.tsx:715` — паттерн «не помещается → скролл» принят системно. |
| **Воспроизведение** | Открыть `/settings/subtitles` на MacBook 14" (1512×982 @ native) → сузить окно до 1200 px → появляется горизонтальная прокрутка на колонке стилей и таблице пресетов. |
| **Метрика** | **1111 строк** кода на двух основных клиентских компонентах субтитров. **10+ полей** в одной вертикали без группировки/табов. **0** адаптивных breakpoint-ов, кроме `sm:` для TopBar. |
| **Корень** | Перегруженный single-column layout + отсутствие tab/accordion-группировки + отсутствие адаптивных grid-шаблонов. IA субтитров не декомпозирован на домены (типографика / цвет / анимация / тайминг / positioning). |
| **Этап лечения** | **08 — Фронт: настройки + Cmd+K** |
| **Целевой чанк** | `REFACTR-52` (разбить субтитры на 5 групп: Текст / Цвет и обводка / Анимация / Позиция / Тайминг; responsive-grid; убрать horizontal scroll; live-preview фиксированной шириной). |

---

### Боль 5 — «Пост-продакшн — все настройки не структурированы»

| Параметр | Значение |
|---|---|
| **Тип** | IA (информационная архитектура) |
| **Цитата** | «Post-production без группировки» (`task.md §1.3`), «все параметры в одной куче» (`README.md:18`) |
| **Доказательство** | `PostProductionSettingsClient.tsx:374–423` — плоская вертикаль из 6 секций подряд без логической группировки: `PresetIdentitySection → IntroOutroSection → AudioNormalizationSection → ZoomSection → VideoEffectsSection → SplitScreenSection`. Нет tabs / accordion / collapsible / step-by-step flow. Разнородные домены (идентичность пресета / ассеты / аудио / зум / эффекты / split-screen) лежат рядом в одной колонке. |
| **Воспроизведение** | `/settings/post-production` → prompt: «настрой громкость и включи зум» → искать scroll-ом по длинному списку. |
| **Метрика** | **6 секций** вертикально без группировки. **766 строк** только на post-production-секциях (без основного клиента). |
| **Корень** | Исторический layout: каждая фича добавлялась последней секцией в конец. Отсутствует группировочная модель «Идентичность → Медиа → Аудио → Видео → Композиция». |
| **Этап лечения** | **08 — Фронт: настройки + Cmd+K** |
| **Целевой чанк** | `REFACTR-53` (IA пост-продакшна: Идентичность / Медиа-ассеты / Аудио / Видео / Композиция; tabs + progressive disclosure; Simple/Expert переключатель). |

---

### Боль 6 — «Экспертный режим / простой мальчиковый»

| Параметр | Значение |
|---|---|
| **Тип** | Функциональный (режимы) |
| **Цитата** | «Simple/Expert режим» (`task.md §2.6`) |
| **Доказательство** | `grep -rn "simple.*expert\|expert_mode\|simpleMode" apps/frontend/src/ apps/backend/src/` = **0 совпадений**. Вся поверхность настроек всегда отображается в экспертной полноте — 8 навигационных пунктов в `SettingsSubNav.tsx:15–59` (Профили / Модели / Производительность / Субтитры / Пост-продакшн / Бренд / Промпты / Аккаунты) × десятки полей внутри каждого. |
| **Воспроизведение** | Открыть `/settings/performance` → 15+ полей (VRAM, батчи, провайдеры, narrative-mode, GPT-chunk, viral-mode-knobs). Человек, который хочет «просто сделать рилсы», упирается в когнитивный перегруз. |
| **Метрика** | **0** mode toggles. **8** разделов настроек. **20+ PRO-специфичных полей** в `runtime_settings` (удаляются в `REFACTR-13`) — даже после удаления у Simple-режима будет ≥30 полей против желаемых ~10. |
| **Корень** | Архитектура настроек не имеет концепта «уровень экспертизы». task.md §2.6 требует 7 смысловых групп + тумблер Simple/Expert, где Simple показывает 1–3 поля на группу, Expert — всё. |
| **Этап лечения** | **08 — Фронт: настройки + Cmd+K** |
| **Целевой чанк** | `REFACTR-51` (IA 7 групп), `REFACTR-56` (Simple/Expert переключатель + persist в `runtime_settings.ui_mode`). |

---

## 2. Боли, найденные в ходе аудита

Эти боли не проговорены владельцем, но обнаружены в ходе REFACTR-00..05 и должны быть учтены.

### Боль 7 (аудит) — Нет автосохранения настроек проекта

| Параметр | Значение |
|---|---|
| **Тип** | Функциональный (persistence) |
| **Обнаружено в** | `docs/audit/04-data-schema.md §5.2`, `docs/audit/05-pipeline-stages.md §3` |
| **Доказательство** | `grep -rn "autosave\|autoSave\|debounced.*save" apps/frontend/src/ apps/backend/src/` = **0 совпадений**. В `models/project.py` 30 строк — минимальные поля (id/name/description/color/timestamps), **нет `settings_snapshot` и `last_saved_at`**. Пользователь должен вручную жать «Сохранить» для каждого пресета. |
| **Воспроизведение** | Открыть `/settings/subtitles`, изменить значение в JSON-редакторе, закрыть вкладку → изменения потеряны. |
| **Метрика** | **0** автосохранений. **0** дебаунсов. 11 разных `*_store.py`-таблиц, каждая требует ручного save (см. `02-settings-inventory.md`). |
| **Этап лечения** | **02 — Бэкенд: чистка и проекты** |
| **Целевой чанк** | `REFACTR-14` (расширение Project: `settings_snapshot` JSON + `stage_progress` + `last_saved_at`), `REFACTR-18` (API автосейв с debounce 10 с + manual save). |

---

### Боль 8 (аудит) — Нет рестарта pipeline с произвольного шага

| Параметр | Значение |
|---|---|
| **Тип** | Функциональный (resumability) |
| **Обнаружено в** | `docs/audit/05-pipeline-stages.md §3` (полная таблица чекпоинтов). |
| **Доказательство** | — `JobStage` enum в `models/job_constants.py` имеет 9 значений (ingest / proxy_generate / transcribe / translate / silence_cut / analyze / render / finalize / done), но **только 2 стадии** (proxy_generate, transcribe) имеют авто-skip через content-addressed cache (`services/pipeline_stages/ingest.py`). <br/>— `services/pipeline.py` вызывает `_run_pipeline_impl` с нуля: `run_ingest_stage → run_analysis_stage → run_render_stage` без параметра `from_stage`. <br/>— `grep -rn "restart.*stage\|restart_from" apps/frontend/src/ apps/backend/src/` = **0 совпадений**. |
| **Воспроизведение** | Job 60-минутного видео упал на стадии `render` из-за OOM/дисковой ошибки → единственный путь — удалить job и создать новый → 40 минут ingest + 15 минут analysis повторяются заново. |
| **Метрика** | **2/9** стадий с checkpoint resume. **7/9** стадий — always-full-rerun. Потеря: до ~55 минут CPU + LLM-стоимость analysis на 60-минутном видео. |
| **Этап лечения** | **02 — Бэкенд: чистка и проекты** |
| **Целевой чанк** | `REFACTR-16` (stage_progress + per-stage loader/skip, `POST /projects/{id}/restart?from_stage=X`), `REFACTR-48` (UI кнопка «Начать заново с шага» в Workbench). |

---

### Боль 9 (аудит) — Нет Студии как главной страницы

| Параметр | Значение |
|---|---|
| **Тип** | Функциональный + IA |
| **Обнаружено в** | `docs/audit/01-frontend-map.md`, `task.md §2.4` |
| **Доказательство** | `apps/frontend/src/app/page.tsx` → `HomeClient.tsx` (83 строки): сверху DashboardHero, посередине UploadWizard, снизу JobList. **Проекты живут отдельно в `/projects`** (`ProjectsDashboard.tsx`, `ProjectsList.tsx`), не появляются на главной. Нет grid превью проектов, нет контекстного меню rename/soft-delete/hard-delete/open-in-Finder на главной. |
| **Воспроизведение** | Открыть `/` → это не «открыл приложение и вижу мои проекты», а «залей новое видео + смотри старые job-ы». |
| **Метрика** | **2 раздельные** страницы (`/` и `/projects`) дублируют ядро сценария вместо единой Студии. |
| **Этап лечения** | **06 — Фронт: Студия и проекты** |
| **Целевой чанк** | `REFACTR-39` (Студия как главная: grid проектов + превью + контекстное меню + modal нового проекта с drop-zone), `REFACTR-40..44` (soft/hard delete, copy-settings-from-другого-проекта, open-in-Finder, rename inline). |

---

### Боль 10 (аудит) — Нет потока идей рилсов (approve/reject/regenerate)

| Параметр | Значение |
|---|---|
| **Тип** | Функциональный (workflow) |
| **Обнаружено в** | `docs/audit/05-pipeline-stages.md §1.3`, `task.md §2.5` |
| **Доказательство** | В `analysis_stage` (`pipeline_stages/analysis.py`) ветки `viral_2026` / `chaptered` / `bottom_up` производят `reel_plan` и **сразу** передают в `render_stage` — без паузы и человеческой валидации. `grep -rn "approve\|reject\|regenerate" apps/frontend/src/components/job/` = **0 совпадений**. Есть `TinderClient.tsx`, но он про ручную оценку уже отрендеренных клипов (апостериори), не про approve/reject ideas до рендера. |
| **Воспроизведение** | Загрузить 60-минутное видео → LLM сгенерирует 10 идей → все 10 рендерятся без вмешательства → 45 минут рендера, из которых 6–7 идей пользователь потом отсеивает. |
| **Метрика** | **0** approval gates перед рендером. До **~70%** потенциальных идей отбрасывается постфактум → кратная перерасходуемая рендер-мощность. |
| **Этап лечения** | **07 — Фронт: Workbench + идеи** |
| **Целевой чанк** | `REFACTR-46` (Grid идей с approve/reject/regenerate + custom prompt), `REFACTR-47` (bridge API `/ideas/{job}/approve` / `reject` / `regenerate`), `REFACTR-49` (pause-между-стадиями в pipeline после analysis в режиме Review). |

---

### Боль 11 (аудит) — 95 backend-сервисов, предположительная мёртвая зона ~20%

| Параметр | Значение |
|---|---|
| **Тип** | Технический долг (поддерживаемость) |
| **Обнаружено в** | `docs/audit/00-backend-map.md`, `docs/audit/03-pro-removal-plan.md` |
| **Доказательство** | `ls apps/backend/src/videomaker/services/` = **95 объектов** (модулей + папок). После `REFACTR-13` (удаление PRO/bottom_up/map_reduce) планируется убрать ≥19 файлов (~7067 LoC). Остальные ~5–10% — кандидаты на мёртвый код (вне `viral_2026` ветки): `preference_memory.py`, `variants_generator.py`, `composition_scorer.py`, `cross_context_risk.py`, `closure_validator.py`, `coherence_validator.py`, `multi_arc_builder.py`. |
| **Воспроизведение** | Новый разработчик (или я в следующей сессии) тратит >15 минут на понимание, какие сервисы реально в pipeline, а какие — артефакты фазы bottom_up/map_reduce. |
| **Метрика** | **95 модулей** в services/ → после чистки ≥19 удалённых → **≤76 живых**. Time-to-orient для инженера падает с ~15 мин до <5 мин. |
| **Этап лечения** | **02 — Бэкенд: чистка и проекты** |
| **Целевой чанк** | `REFACTR-13` (удаление PRO + bottom_up/map_reduce + orphaned сервисы), `REFACTR-15` (audit dead-code остатков + удаление). |

---

## 3. Матрица «боль → тип → этап → чанк»

| # | Боль | Тип | Этап | Чанк(и) |
|---|------|-----|------|---------|
| 1 | OOM 12 ГБ dev-фронт | тех | 04 | REFACTR-27, 28, 31 |
| 2 | Смешанные тёмные/светлые цвета (light-mode leaks) | виз | 05 | REFACTR-33, 37 |
| 3 | Cmd+K не работает | функц | 08 | REFACTR-57 |
| 4 | `/settings/subtitles` horizontal scroll | виз/IA | 08 | REFACTR-52 |
| 5 | Post-production плоская IA | IA | 08 | REFACTR-53 |
| 6 | Нет Simple/Expert режима | функц | 08 | REFACTR-51, 56 |
| 7 | Нет автосохранения (владелец: §2.6) | функц | 02 | REFACTR-14, 18 |
| 8 | Нет рестарта pipeline с шага | функц | 02 | REFACTR-16 (+ UI: REFACTR-48) |
| 9 | Нет Студии на главной | IA | 06 | REFACTR-39..44 |
| 10 | Нет approve/reject/regenerate идей | функц | 07 | REFACTR-46, 47, 49 |
| 11 | 95 backend-модулей, ~20% мёртвой зоны | тех долг | 02 | REFACTR-13, 15 |

---

## 4. Приоритезация (очередь лечения после аудита)

Логика: сначала блокеры работы (OOM, потеря данных), затем фундамент (дизайн-система), затем UX-слой поверх.

| Очередь | Боль | Мотивация |
|---|------|-----------|
| 1 | #1 OOM 12 ГБ | Блокирует нормальную разработку. Миграция на Vite снимает ceiling и делает dev-цикл быстрее в 3–5×. |
| 2 | #7 Автосохранение + #8 Рестарт с шага | Без них любой сбой = потеря часа работы на 60-мин видео. Бэкенд-основа для всего UX. |
| 3 | #11 Dead-code чистка | Открывает безопасное поле для следующих правок бэкенда (REFACTR-14..20). |
| 4 | #2 Mixed theming → дизайн-система | Фундамент для всех UI-чанков этапов 06–08. Без unified tokens нельзя чистить IA. |
| 5 | #9 Студия | Возвращает ядро сценария пользователю. |
| 6 | #10 Approve/reject идей + #6 Simple/Expert | UX-слой: контроль над pipeline + снижение когнитивной нагрузки. |
| 7 | #4 Subtitles IA + #5 Post-production IA | Финишная доводка настроек. |
| 8 | #3 Cmd+K | Иконка продуктовой зрелости; реализуется поверх устаканенной навигации. |

---

## 5. GATE-чекпоинт

- [x] Минимум 6 болей задокументировано (зафиксировано **11**: 6 от владельца + 5 из аудита).
- [x] Для каждой — техническое доказательство (`file:line` или цитата).
- [x] Матрица «боль × этап × чанк» заполнена.
- [x] Приоритезация построена (8 уровней очереди лечения).
- [x] Итоговая запись в Лог изменений `PIPELINE-НАВИГАТОР.md` добавлена.
- [x] **Этап 00 ЗАВЕРШЁН** — все 7 чанков (REFACTR-00..06) выполнены. Переход к Этапу 01 (Архитектурные решения, REFACTR-07).

---

## 6. Следующий шаг

**REFACTR-07 (Этап 01, чанк 8/67)** — ADR-07 Frontend-стек: Vite 6 vs Next.js 16 для локального single-user SPA. Решение формализуется как ADR в `docs/adr/ADR-07-frontend-stack.md` и подпирает чанки REFACTR-27..31 миграции фронта.
